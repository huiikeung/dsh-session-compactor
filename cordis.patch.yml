# dsh-session-compactor bundle 层：插入一行装配，默认配置开箱即用。
# 自定义阈值时把 config: {} 替换为：
#   config:
#     thresholdRatio: 0.7
#     retainRatio: 0.2
#     maxOverflowRetries: 3
- insert:
    - id: dsh-session-compactor
      name: 'dsh-session-compactor'
      config: {}
